1. Dependencies:

```bash
pip install dash plotly
```
2. Run dashboard: 

```bash
python -m src.dashboard.app
```

---

Prevent auto-opening browser:

```bash
python -m src.dashboard.app --no-browser
```

Use any port:

```bash
python -m src.dashboard.app --host 0.0.0.0 --port 8051
```